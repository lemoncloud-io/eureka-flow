/**
 * Moved to `@flows/engine` (`core/ids.ts`). Kept here so callers that import the file
 * directly keep working; remove once nothing imports this path.
 */
export { newEdgeId, newNodeId } from '@flows/engine';
