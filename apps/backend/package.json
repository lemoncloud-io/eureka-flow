{
    "name": "@flows/backend",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "start": "npx serverless offline --httpPort 8800 --websocketPort 8801 --noPrependStageInUrl",
        "deploy:dev": "npx serverless deploy --stage dev",
        "deploy:prod": "npx serverless deploy --stage prod"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.700.0",
        "@aws-sdk/lib-dynamodb": "^3.700.0",
        "@aws-sdk/client-s3": "^3.700.0",
        "@aws-sdk/client-apigatewaymanagementapi": "^3.700.0",
        "ulid": "^2.3.0",
        "zod": "^3.24.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.145",
        "serverless": "^3.40.0",
        "serverless-esbuild": "^1.52.0",
        "serverless-offline": "^13.8.0",
        "esbuild": "^0.24.0"
    }
}
