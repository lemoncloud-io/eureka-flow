# eureka-flows-front

data flow engine

## HOW TO DEVELOP

1. install yarn. `npm i -g yarn`
2. install dependencies. `yarn install`
3. initial `.env.local` as `cp .env.example .env.local`
4. (optional) run `eureka-flows-api` in local.
5. run front-end `npm run dev`.
